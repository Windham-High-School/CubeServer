#/etc/dnsmasq.conf

#domain-needed
#bogus-priv
#expand-hosts

no-resolv
#server=/localnet/${DHCP_ROUTER_ADDR}
#dhcp-range=${DHCP_RANGE_LOW},${DHCP_RANGE_HIGH},${DHCP_MAX_LEASE_TIME}h

# The address is the static IP of this server
# You can find this ip by running ifconfig and look for the
# IP of the interface which is connected to the router.
listen-address=${DHCP_ROUTER_ADDR}
listen-address=127.0.0.1
listen-address=255.255.255.255
bind-interfaces

# Create custom 'domains'.
# Custom 'domains' can also be added in /etc/hosts
#address=/${API_ADDR}/${DHCP_ROUTER_ADDR}
